/**
 * Re-export shared API types from @ecommerce/http.
 * All existing imports of `@/types/api` continue working unchanged.
 */
export type {
  ApiList,
  ApiResponse,
  PaginatedDataList,
  PaginatedList,
  PaginationMeta,
} from "@ecommerce/http"
